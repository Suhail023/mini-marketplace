meta {
  name: Register
  seq: 1
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: none
}

json {
  "email": "testuser@example.com",
  "password": "securepass123",
  "first_name": "Test",
  "last_name": "User"
}

script {
  res: util_variable.set("accessToken", res.body.access_token)
  res: util_variable.set("userId", res.body.user_id)
}
